

<nav class="navbar navbar-expand-sm bg-dark navbar-dark">
  <!-- Brand/logo -->
  <a class="navbar-brand" href="{{ siteUrl }}"> 
    <i class="fas fa-balance-scale"></i>
  </a> 
  
  <!-- Links -->
  <ul class="navbar-nav">
    <li class="nav-item">
      <a class="nav-link active" href="{{ siteUrl }}">Προσφυγές</a>
    </li>
    {% for result in craft.categories().group('appealtype').all() %}

        {% if result.title == 'Δεκτή' %}

             {% set item='Δεκτές' %}

        {% elseif result.title == 'Απορριπτέα' %}

             {% set item='Μη Δεκτές' %}

        {% endif %}

        <li class="nav-item">
          <a class="nav-link" href="{{result.url}}">{{ item }}</a>
        </li>      

    {% endfor %}    

    
     <li class="nav-item">
      <a class="nav-link" href="{{url('search/index')}}"><i class="fas fa-search"></i>Αναζήτηση</a>
    </li>
  </ul>
</nav>